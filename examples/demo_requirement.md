# Synthetic Order Eligibility Requirement

R1. An order with a positive amount and an active account is eligible.
R2. The same order identifier must not produce duplicate active decisions.
R3. Existing environment records may be used as read-only reference data.
